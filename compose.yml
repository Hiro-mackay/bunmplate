services:
  postgres:
    image: postgres:18-alpine
    container_name: bunmplate-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bunmplate
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
